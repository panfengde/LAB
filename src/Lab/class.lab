 (class animal
        (constructor (color_1  wheel_1)
            (define color color_1)
            (define wheel wheel_1)
        )
        (define (a x) (+ x 100))
        (define (b x) (+ x 100))
    )

    (define one (new animal "aaa" 11111))

    one.color
    one.wheel

    (one.a 10)